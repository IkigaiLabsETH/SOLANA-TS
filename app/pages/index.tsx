import type { NextPage } from "next";
import Head from "next/head";
// import Home from "../app/jupiter/Home";

const Index: NextPage = (props) => {
  return (
    <div>
      <Head>
        <title>Jupiter Swap</title>
        <meta
          name="description"
          content="This site will fly high 🦤"
        />
      </Head>
    </div>
  );
};

export default Index;